# JWT_SECRET_KEY: A secret key used for signing and verifying JSON Web Tokens (JWTs).
# This should be a long, random, and securely stored string in production.
# If not set, a default value will be used (only for development/testing).
JWT_SECRET_KEY="your_super_secret_jwt_key_here"

# FRONTEND_ORIGIN: The URL of the frontend application allowed to access this backend API.
# This is used for Cross-Origin Resource Sharing (CORS) configuration.
# Example: http://localhost:3001 (for local development)
FRONTEND_ORIGIN="http://localhost:3001"